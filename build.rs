// build.rs — minimal build script.
// The vortex-jit crate (v0.7.1+) has the extended CALL_RUNTIME callback
// built in, so no patching is needed.

fn main() {
    // Nothing to do — the vortex-jit crate handles everything.
}
